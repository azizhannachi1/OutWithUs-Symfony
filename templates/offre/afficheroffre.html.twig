{% extends 'base.html.twig' %}


{% block body %}
<br><br><br><br><br><br>
<section id="main-content">
    <section class="wrapper site-min-height">
        <h1>Liste des Offres</h1>
        <button class="btn btn-success btn-icon-split" ><a href="{{ path('ajouteroffre')  }}"><span class="text" style="color: white">Ajouter </span></a></button>

    <table class="table table-bordered" id="t"  width="100%" cellspacing="0">
        <thead>
        <tr>
            <th> </th>
            <th> </th>
            <th> Nom  </th>

            <th>Date </th>
            <th> description</th>

        </tr>
        </thead>

        <tbody>
        <tbody id="myTable">

        {% for i in tableoffre %}
            <tr>



                <td><a href="{{ path('supprimeroffre',{'id': i.id}) }}"><img src="https://img.icons8.com/color/48/000000/trash--v2.png"/></a></td>
                <td>  <a href="{{ path('modifieroffre',{'id': i.id}) }}"  class="btn btn-info btn-icon-split">
                                            <span class="icon text-white-50">
                        <i class="fas fa-info-circle"></i>

                    </span>
                        <span class="text">Modifier</span>

                    </a></td>

                <td>{{ i.Nom }}</td>


                <td>{{(i.date).format('d/m/Y')}}</td>
                <td>{{ i.description }}</td>


            </tr>
        {% endfor %}

        </tbody>
        </tbody>
        <tbody id="search">
        </tbody>

    </table>

    </section>
    <!-- /wrapper -->
</section>
{% endblock %}
